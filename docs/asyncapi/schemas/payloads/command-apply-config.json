{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "./command-apply-config.json",
  "title": "Apply config command",
  "allOf": [
    {
      "$ref": "../messages/command.json"
    },
    {
      "type": "object",
      "required": [
        "action",
        "data"
      ],
      "properties": {
        "action": {
          "const": "apply_config"
        },
        "data": {
          "type": "object",
          "required": [
            "config_hash",
            "heartbeat_interval_seconds",
            "adapter_type",
            "feedback_type",
            "compartments"
          ],
          "properties": {
            "config_hash": {
              "type": "string",
              "pattern": "^[A-Fa-f0-9]{64}$",
              "description": "SHA-256 of the compact canonical JSON object containing exactly adapter_type, feedback_type, and compartments in that key order; compartments are sorted by compartment_number."
            },
            "heartbeat_interval_seconds": {
              "type": "integer",
              "minimum": 1
            },
            "adapter_type": {
              "type": "string",
              "enum": [
                "waveshare_modbus",
                "rs485_lock_board"
              ]
            },
            "feedback_type": {
              "type": "string",
              "enum": [
                "door_closing",
                "door_opening"
              ]
            },
            "compartments": {
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "compartment_number",
                  "slaveId",
                  "address"
                ],
                "properties": {
                  "compartment_number": {
                    "type": "integer",
                    "minimum": 1
                  },
                  "slaveId": {
                    "type": "integer",
                    "minimum": 1
                  },
                  "address": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 254,
                    "description": "Zero-based channel address; must be wire-encodable as a non-zero one-byte channel (0 maps to wire channel 1, maximum 254)."
                  }
                },
                "additionalProperties": false
              }
            }
          },
          "additionalProperties": true
        }
      }
    }
  ]
}
