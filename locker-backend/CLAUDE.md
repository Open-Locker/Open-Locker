# Backend agent pointer

For backend work, use the `backend-laravel` skill and load
`open-locker-domain` for architecture or ADR decisions. The canonical guidance
lives in `.agents/skills/`; this file exists for Claude compatibility.
