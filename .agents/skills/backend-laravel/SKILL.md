---
name: backend-laravel
description: Use for work under locker-backend/ involving Laravel, Filament, PHP, API resources, event-sourced workflows, or backend tests.
---

Read `locker-backend/composer.json` for authoritative versions. Follow sibling
code, strict types, explicit return types, thin controllers, Form Requests,
Policies, Services, JsonResources, and event-sourcing boundaries. Mock hardware
services in feature tests. Run the narrowest affected PHPUnit, Pint, and static
analysis checks available. Load `api-contract-sync` for API changes and
`open-locker-domain` for architecture decisions.
