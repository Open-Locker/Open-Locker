# Mobile agent pointer

For mobile work, use the `mobile-app` skill. Load `api-contract-sync` whenever
an API shape or generated client is involved. The canonical guidance lives in
`.agents/skills/`; this file exists for Claude compatibility.
