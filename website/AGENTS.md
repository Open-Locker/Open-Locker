# Website agent pointer

This is the static Astro site deployed to GitHub Pages. Keep routes/content
lightweight and accessible, and keep backend business logic out of `website/`.
Use the scripts in `website/package.json` for verification.
