# Open-Locker agent guidance

Use [AGENTS.md](AGENTS.md) as the repository map. Detailed guidance lives in
`.agents/skills/` and should be loaded only when its topic applies. Check the
source tree and package manifests before trusting older documentation.
